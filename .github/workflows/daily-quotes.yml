name: Daily Quotes

on:
  schedule:
    # المواعيد بتوقيت UTC (زود ساعتين لمصر)
    - cron: "0 9 * * *" # بيشتغل تلقائي كل يوم الساعة 9 بتوقيت جرينتش (11 صباحاً بتوقيت مصر)
    - cron: "0 17 * * *" # بيشتغل تلقائي كل يوم الساعة 17 بتوقيت جرينتش (7 مساءاً بتوقيت مصر)
  workflow_dispatch: # للسماح بالتشغيل اليدوي للتجربة

jobs:
  send_quote:
    runs-on: ubuntu-latest
    steps:
      - name: Hit API with Bypass
        run: |
          for attempt in {1..3}; do
            echo "Attempt $attempt..."
            curl -i -X GET "https://abona-faltaus.vercel.app/api/daily-quote" \
              -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
              -H "x-vercel-protection-bypass: ${{ secrets.VERCEL_BYPASS_TOKEN }}" \
              -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/120.0.0.0" \
              -H "Referer: https://abona-faltaus.vercel.app/" \
              -H "Accept: application/json" && break

            if [ $attempt -lt 3 ]; then
              echo "Retrying in 5 seconds..."
              sleep 5
            fi
          done
