# خطة مراجعة وتحسين أداء Lighthouse الشاملة (Desktop & Mobile)

> **ملخص نتائج التقرير الفعلي:**
> - **Desktop**: الأداء (Performance): `53/100` | إمكانية الوصول (Accessibility): `100/100` | أفضل الممارسات (Best Practices): `73/100` | تحسين محركات البحث (SEO): `100/100`
> - **Mobile**: الأداء (Performance): `41/100` | إمكانية الوصول (Accessibility): `100/100` | أفضل الممارسات (Best Practices): `73/100` | تحسين محركات البحث (SEO): `100/100`

---

## 1. تحليل أسباب انخفاض الأداء (Performance Bottlenecks)

### أ. سرعة الاستجابة والتحميل (LCP & TBT):
1. **LCP (Largest Contentful Paint)**:
   - ديسكتوب: `3.2s` | موبايل: `8.2s` (الهدف المطلوب: أقل من `2.5s`).
   - **السبب**: تأخر تحميل صورة الهيدر/الشعار الكبيرة واعتمادها على سكريبتات ثقيلة في الـ Main Thread قبل الرسم، بالإضافة لعدم تفعيل `priority` و `preload` على عنصر الـ LCP الأساسي.
2. **TBT (Total Blocking Time)** و **Main Thread Work**:
   - ديسكتوب: `560ms` | موبايل: `3,380ms` (الهدف المطلوب: أقل من `200ms`).
   - **السبب الرئيسي**: حجم الـ JavaScript الضخم (`Unused JavaScript` بأكثر من `1.9 MB` على الديسكتوب و `1.3 MB` على الموبايل).
   - سكريبتات خارجية مثل `OneSignal SDK`، `Google Tag Manager / Analytics`، ومكتبات ضخمة تعمل عند فتح الصفحة مباشرة بدلاً من التحميل الكسول (Dynamic Import / Lazy Load).

### ب. أفضل الممارسات (Best Practices - 73/100):
1. **أخطاء الكونسول (Errors in console)**:
   - وجود أخطاء في الـ runtime و Service Worker postMessage تؤدي لخصم نقاط Best Practices بالكامل.
2. **كوكيز الطرف الثالث (Third-party Cookies)**:
   - ناتجة من بعض خدمات التتبع والمكتبات الخارجية غير المحسنة.
3. **عدم استعادة الكاش العكسي (Back/Forward Cache Restoration)**:
   - مستمعي أحداث `unload` أو اتصالات مفتوحة غير مغلقة تعيق ميزة `bfcache` في متصفحات Chrome و Safari.

---

## 2. خطة التعديلات والتحسينات الجذرية

| البند | المشكلة | الحل الفني وخطة التنفيذ | الأثر المتوقع |
| :--- | :--- | :--- | :--- |
| **1. تأجيل السكريبتات الخارجية (Third-party Scripts)** | OneSignal, Google Tag | تأجيل تحميل OneSignal و Google Analytics حتى اكتمال تفاعل المستخدم الأول (`requestIdleCallback` أو `user interaction`) بدلاً من التحميل الفوري في الـ Head. | انخفاض TBT بأكثر من 60% وتحسن LCP |
| **2. التقسيم الكودي (Code Splitting & Dynamic Imports)** | حزم JS زائدة عن الحاجة (`1.9 MB`) | تحويل المودالات والمكونات الثقيلة (مثل مكتبات المحادثة، محرر النغمات، المودالات المعقدة) إلى `next/dynamic` مع `ssr: false` للمكونات التفاعلية فقط. | تقليل حجم البندل الابتدائي وتسريع FCP و TTI |
| **3. تحسين صور وشعار الـ LCP** | تأخر ظهور عنصر LCP الأساسي | استخدام مكوّن `next/image` مع إعطاء خاصية `priority={true}` و `fetchPriority="high"` وإضافة وسم `<link rel="preload">` للصورة الرئيسية. | خفض LCP إلى أقل من 2.0 ثانية على الهاتف والكمبيوتر |
| **4. تنظيف مستمعي أحداث المتصفح للـ bfcache** | عدم عمل BFCache ووجود تحذير Permissions policy unload | إزالة أي ربط لـ `unload` واستبدالها بـ `pagehide` أو `visibilitychange` لتمكين الـ BFCache الفوري. | رفع Best Practices إلى 100/100 |
| **5. التخلص من أخطاء الكونسول** | أخطاء JavaScript تظهر للـ Audit | معالجة جميع الأخطاء غير الملتقطة في صفحات السنكسار والـ Service Worker لضمان كونسول نقي 100%. | تثبيت نتيجة Best Practices في المنطقة الخضراء |

---

## 3. مؤشرات الأداء المستهدفة بعد التنفيذ (Target Metrics)

| المقياس (Metric) | النتيجة الحالية (موبايل) | النتيجة المستهدفة (موبايل) | النتيجة الحالية (ديسكتوب) | النتيجة المستهدفة (ديسكتوب) |
| :--- | :---: | :---: | :---: | :---: |
| **الأداء (Performance)** | **41** | **90+** | **53** | **95+** |
| **إمكانية الوصول (Accessibility)** | **100** | **100** | **100** | **100** |
| **أفضل الممارسات (Best Practices)** | **73** | **100** | **73** | **100** |
| **محركات البحث (SEO)** | **100** | **100** | **100** | **100** |
| **LCP (Largest Contentful Paint)** | 8.2s | < 2.5s | 3.2s | < 1.2s |
| **TBT (Total Blocking Time)** | 3,380ms | < 200ms | 560ms | < 50ms |
