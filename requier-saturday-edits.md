# قائمة المهام والتعديلات المطلوبة — Saturday Edits
تم استخراج جميع المتطلبات من ملف [saturday-edits.md](file:///p:/Projects/abona-faltaus/saturday-edits.md) وتصنيفها بشكل دقيق ومنظم لتسهيل التنفيذ والمتابعة.

---

## 1. المتطلبات والمهام البرمجية (حسب الصفحة والميزة)

### 📌 أولاً: صفحة النوتة الذكية (Smart Notes)
1. **الفرق بين ملف Word و PowerPoint في التوليد والتحميل:**
   - **Word (.docx):** شرح تفصيلي مقروء للخادم مقسم لفقرات كاملة (مقدمة، أفكار، تفسير، تطبيق، خلاصة).
   - **PowerPoint (.pptx):** شرائح عرض مختصرة للنقاط الأساسية والآيات فقط، كل شريحة فكرة واحدة.
   - **PDF (.pdf):** نسخة منسقة للطباعة والمشاركة مبنية على قالب HTML مدعوم للعربية.

2. **برومبت أرثوذكسي صارم (Strict Orthodox System Prompt):**
   - إنشاء ملف مخصص للبرومبت `lib/notes-prompt.ts` مقيد بالعقيدة الأرثوذكسية وتفاسير الآباء المعتمدة (القمص تادرس يعقوب ملطي، القمص أنطونيوس فكري، كنيسة مارمرقس بمصر الجديدة، موقع سانت تكلا ومناهج الكرازة).

3. **تنظيف مخرجات الذكاء الاصطناعي (بدون مقدمة وخاتمة):**
   - إلزام الـ AI بالبدء المباشر بالمحتوى وحذف أي مقدمات (مثل: "بالتأكيد"، "إليك الدرس") أو خواتم توديعية، مع فلترة نصية في الـ API كحماية إضافية.

4. **خيارات المحتوى الاختيارية (Checkboxes في الواجهة):**
   - إضافة خيارات تفعيل/تعطيل قبل التوليد: (آيات كتابية، أقوال آباء، صلاة افتتاحية/ختامية، نشاط/تطبيق عملي، خلاصة).

5. **شريط جانبي لحفظ واسترجاع الدروس (Lessons Sidebar):**
   - إنشاء جدول `lesson_notes` في Supabase مع سياسات الأمان (RLS).
   - بناء مكون `components/notes/LessonsSidebar.tsx` لإدارة الدروس المحفوظة، استعراضها، حذفها، أو إنشاء درس جديد.
   - حفظ الدرس تلقائياً في قاعدة البيانات فور توليده وربطه بحساب المستخدم.

6. **إضافة تفاصيل إضافية من الخادم (Extra Details):**
   - حقل نصي إضافي في الواجهة لإدخال ملاحظات خاصة بالخادم (مثل ظروف معينة للمخدومين أو التركيز على نقطة بعينها) لتمريرها في البرومبت.

7. **إصلاح تصدير ملفات الـ PDF مع دعم اللغة العربية:**
   - حل مشكلة الخطوط والـ RTL عبر تجهيز قالب HTML متكامل مع خطوط عربية وتصديره عبر `html2pdf.js`.

8. **إضافة مصادر ومراجع مخصصة (Custom Sources):**
   - تمكين الخادم من إضافة روابط أو نصوص مراجع خارجية ليعتمد عليها الذكاء الاصطناعي أثناء التحضير.

9. **التسجيل الصوتي مع آليات احتياطية (Voice Recording & Fallbacks):**
   - بناء مكون `components/notes/VoiceRecorderButton.tsx`.
   - الاعتماد بالترتيب على:
     1. Groq Whisper (سريع ومجاني للغة العربية).
     2. ElevenLabs STT (كخيار احتياطي).
     3. Web Speech API على متصفح المستخدم (Fallback أخير عند تعذر الـ API).

---

### 📌 ثانياً: صفحة الألحان (Hymns Page)
10. **الأعمدة الديناميكية للغات (Dynamic Columns):**
    - تعديل عرض كلمات الألحان في `app/al7an/page.tsx` بحيث إذا أخفى المستخدم لغة أو اثنتين (عربي، قبطي، معرب) يتغير الـ Grid ديناميكياً (`grid-cols-1`, `grid-cols-2`, `grid-cols-3`) وتأخذ النصوص المساحة الكاملة.
    - إضافة حماية تمنع إلغاء تحديد جميع اللغات في نفس الوقت.

---

## 2. جدول الملفات المطلوبة للإنشاء والتعديل

| المسار | نوع العملية | الوصف والهدف |
| :--- | :---: | :--- |
| `lib/notes-prompt.ts` | **إنشاء جديد** | برومبت النظام الأرثوذكسي المقيد للنوتة الذكية |
| `components/notes/LessonsSidebar.tsx` | **إنشاء جديد** | القائمة الجانبية لعرض وحذف وفتح الدروس المحفوظة |
| `components/notes/VoiceRecorderButton.tsx` | **إنشاء جديد** | زر ومسجل الصوت مع معالجة الـ Fallback |
| `app/api/notes/generate/route.ts` | **إنشاء جديد** | API توليد محتوى الدرس وتنظيف المقدمة والخاتمة |
| `app/api/notes/generate-pdf/route.ts` | **إنشاء جديد** | API تجهيز قالب الـ HTML المنسق للـ PDF العربي |
| `app/api/notes/generate-pptx/route.ts` | **إنشاء جديد** | API توليد شرائح PowerPoint عبر مكتبة `pptxgenjs` |
| `app/api/notes/transcribe/route.ts` | **إنشاء جديد** | API تفريغ الصوت لنص عبر Groq Whisper / ElevenLabs |
| `app/notes/page.tsx` | **تعديل / تطوير** | واجهة النوتة الذكية ودمج كل العناصر الجديدة |
| `app/al7an/page.tsx` | **تعديل** | تصحيح الـ Dynamic Columns وحماية الـ Checkboxes |

---

## 3. إعدادات قاعدة البيانات (Supabase SQL)

المطلوب تشغيل السكربت التالي في Supabase SQL Editor:
```sql
CREATE TABLE lesson_notes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  title TEXT NOT NULL,
  note_content TEXT,
  requirements JSONB,
  options JSONB,
  generated_content TEXT,
  extra_sources TEXT[],
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

ALTER TABLE lesson_notes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "user_owns_lesson" ON lesson_notes
  USING (auth.uid() = user_id);
```

---

## 4. الحزم والمكتبات المطلوبة للتركيب (NPM Packages)

المطلوب تثبيت المكتبات التالية:
```bash
npm install pptxgenjs html2pdf.js
```

---

## 5. متغيرات البيئة المطلوبة (.env.local)

المطلوب توفير وإضافة المفاتيح التالية:
```env
GROQ_API_KEY=gsk_...              # لميزة تحويل الصوت لنص السريع بدقة عربية عالية
ELEVENLABS_API_KEY=...            # اختياري كـ Fallback للـ STT
```

---

## 6. الترتيب الزمني الموصى به للتنفيذ
`10 (الألحان)` ⬅️ `3 (تنظيف المخرجات)` ⬅️ `4 (خيارات المحتوى)` ⬅️ `2 (البرومبت الأرثوذكسي)` ⬅️ `7 (إصلاح PDF)` ⬅️ `9 (التسجيل الصوتي)` ⬅️ `5 (قاعدة البيانات والـ Sidebar)` ⬅️ `6 (التفاصيل الإضافية)` ⬅️ `8 (المصادر الخارجية)` ⬅️ `1 (توليد Word & PPTX)`.
