# سجل المشاكل والأخطاء البرمجية والمنطقية (Error Register)

تم فحص ومراجعة كافة صفحات الموقع ومكوناته ومنطق العمل والأزرار وقواعد البيانات والتوجيه (Routing) وحصر المشاكل والحلول المطبقة:

---

### 1. خطأ الكتاب المقدس القبطي: حقن نص عربي في العرض القبطي (Coptic Bible Fallback Bug) [تم الحل ✅]
- **الموقع:** `lib/bible-utils.ts` و `components/bible/VerseItem.tsx`.
- **المشكلة السابقة:** عند استدعاء `loadCopticBible()`، تم وضع كود تعويضي للأسفار المفقودة في الترجمة القبطية (`1sm`, `2sm`, `1ki`, `2ki`, `1ch`, `2ch`, `ezr`, `ne`) يقوم بجلب النص العربي من `bible_fixed.json` ووضعه داخل مصفوفة القبطي `text_vocalized`.
- **النتيجة:** عند اختيار القبطي واختيار "صموئيل الأول"، كان يظهر نص عربي مكتوب باتجاه اليسار إلى اليمين (LTR) وبخط قبطي ونقاط مقلوبة.
- **الإصلاح المنفذ:** تم حذف حقن النص العربي نهائياً من بيانات الكتاب المقدس القبطي؛ وتحديث مفتاح كاش المتصفح في `localforage` إلى `offline_coptic_bible_data_v2` لمنع تحميل البيانات المعطوبة القديمة من جهاز العميل.

---

### 2. خطأ فادح في روابط صفحات الخصوصية والمراجعات والشروط (Broken Lucide Link Import) [تم الحل ✅]
- **الموقع:** 
  - `app/[locale]/review/page.tsx`
  - `app/[locale]/privacy/page.tsx`
  - `app/[locale]/terms/page.tsx`
  - `app/[locale]/about/page.tsx`
- **المشكلة السابقة:** استيراد `import { Link } from "lucide-react"` بدلاً من استيراد `Link` من `@/i18n/navigation`.
- **النتيجة:** أزرار الرجوع للرئيسية كانت تنهار أو تعرض كأيقونات معطلة غير قابلة للتنقل.
- **الإصلاح المنفذ:** تم تصحيح الاستيراد في كافة الملفات الأربعة لاستخدام `import { Link } from "@/i18n/navigation"`.

---

### 3. روابط مباشرة تتجاهل لغة الموقع وتحدث إعادة تحميل كاملة (Hardcoded Native Anchors) [تم الحل ✅]
- **الموقع:** `components/home/MysteriousExperience.tsx` (السطر 269).
- **المشكلة السابقة:** استخدام `<a href="/agpeya">` مباشر بدلاً من مكون `Link` الدولي `@/i18n/navigation`.
- **النتيجة:** عند النقر عليها كان يفقد الموقع سياق المسار الدولي `/en` ويحدث Reload كامل للصفحة وتفريغ لحالة المتصفح.
- **الإصلاح المنفذ:** تم استبداله بـ `<Link href="/agpeya">` من `@/i18n/navigation`.

---

### 4. نظام المسافات والمقاسات العام (Tailwind Spacing System) [تم إيقاف التعديل حسب التوجيه ⏸️]
- **الموقع:** `app/globals.css` وجميع ملفات المشروع (`.tsx`).
- **الملاحظة:** قيمة `--spacing: 1rem;` محددة في ملف globals ومبني عليها كل قياسات الواجهات الحالية في أكثر من 100 ملف. بناءً على تعليماتك، لم يتم المساس بها تجنباً لتخريب أبعاد التصميم الحالية للمشروع بالكامل، حتى يتم تنفيذ خطة شاملة وموسعة لها لاحقاً.

---

### 5. تخزين الكاش القديم في LocalForage يمنع وصول التحديثات (Stale LocalForage Cache) [تم الحل ✅]
- **الموقع:** `app/[locale]/bible/page.tsx`.
- **المشكلة السابقة:** استخدام مفتاح كاش قديم ثابت `offline_coptic_bible_data`.
- **الإصلاح المنفذ:** تم رفع مفتاح الكاش إلى `offline_coptic_bible_data_v2` في كل من مرحلة التهيئة الأولية ودالة تبديل اللغة `handleLanguageChange` لضمان تحميل البيانات النظيفة في أجهزة الزوار فوراً.

---

### 6. استخدام تنبيهات المتصفح البدائية المعلقة للواجهة (Native `alert()` Usage) [تم الحل ✅]
- **الموقع:** `components/AdminReviewsClient.tsx` و `app/[locale]/review/ReviewClient.tsx`.
- **المشكلة السابقة:** استدعاء `alert(...)` المتزامنة التي تجمد الواجهة وتظهر بشكل بدائي.
- **الإصلاح المنفذ:** استبدال كافة التنبيهات بـ `toast.success` و `toast.error` التفاعلية والحديثة من مكتبة `sonner`.

---

### 7. توجيه تسجيل الدخول والخروج يتجاهل بادئة اللغة (Auth Routing Without Locale) [تم الحل ✅]
- **الموقع:** `app/[locale]/auth/signin/page.tsx` و `app/[locale]/auth/signup/page.tsx`.
- **المشكلة السابقة:** استخدام `useRouter` و `Link` من `next/navigation` و `next/link`.
- **الإصلاح المنفذ:** تم استبدالها بـ `Link` و `useRouter` من `@/i18n/navigation` لضمان الاحتفاظ بمسار اللغة النشط للمستخدم (`/ar` أو `/en`).

---

### 8. زر المحادثة (AI Chat) في القائمة الدائرية لا يوجه لصفحة الدخول عند عدم التسجيل [تم الحل ✅]
- **الموقع:** `components/home/HomeClient.tsx`.
- **المشكلة السابقة:** عند نقر زائر غير مسجل على زر محمي، كان يكتفي بعرض toast دون إرشاده أو نقله لصفحة تسجيل الدخول.
- **الإصلاح المنفذ:** إضافة توجيه تلقائي سلس `router.push('/auth/signin')` عند النقر مع إظهار التنبيه.
