# دليل نظام الثيمات والألوان المتكامل (theme-full.md)

يوثق هذا الملف نظام الثيمات الموحد لموقع أبونا فلتاؤس السرياني لضمان اتساق بصري، وضوح عالي للنصوص (High Contrast)، وتجربة قبطية تراثية ملكية في جميع الصفحات.

---

## 1. الأوضاع المعتمدة في النظام (Theme Modes)

يدعم التطبيق 3 أنماط تصميمية رئيسية تتكيف مع تفضيلات المستخدم:

1. **الوضع التراثي الفاتح (Coptic Parchment / Light Mode)**:
   - مستوحى من لون المخطوطات والجلد الطبيعي العتيق للأديرة.
   - يعتمد على درجات البيج والـ Warm Amber مع نصوص بلون كستنائي داكن وعناوين مذهبة.
2. **الوضع الليلي الحديث (Oled & Dark Mode)**:
   - خلفيات عميقة من الزنك والأسود المريح للعين أثناء القراءات والصلوات الليلية.
   - إبراز العناصر المهمة بدرجات الذهب والكهرمان النير.
3. **الوضع القبطي المذهب الملكي (Coptic Gold Mode)**:
   - خلفية بنية فحمية غنية بالأصالة مع إطارات وهالات من الذهب الخالص (`#d4af37`).

---

## 2. جدول رموز الألوان القياسية (Color Design Tokens)

| الرمز (Token) | الوضع الفاتح (Light) | الوضع الليلي (Dark) | الوضع الذهبي (Gold) | الوصف والاستخدام |
| :--- | :--- | :--- | :--- | :--- |
| `--background` | `#E7D0AE` (ورق المخطوطات) | `#121214` (زنك فاحم) | `#1C150C` (بني ديري عتيق) | الخلفية الرئيسية لكامل صفحات الموقع |
| `--foreground` | `#2D1E18` (كستنائي داكن مقروء) | `#F4F4F5` (أبيض ناصع هادئ) | `#FEF6E4` (عاجي مذهب) | لون النصوص الأساسية والفقرات |
| `--card` | `#FDFBF7` (أبيض دافئ مصفى) | `#1E1E24` (رمادي داكن ناعم) | `#271E11` (كستنائي غامق) | خلفية البطاقات والقوائم والمودالات |
| `--primary` | `#6B1124` (أحمر قرمزي كنسي) | `#EAB308` (أصفر ذهبي ساطع) | `#D4AF37` (ذهب قبطي خالص) | الأزرار الرئيسية، الأيقونات النشطة، التحديد |
| `--secondary` | `#E2CBA8` (بيج دافئ متوسط) | `#27272A` (زنك رمادي) | `#3B2C17` (بني برونزي) | البطاقات الفرعية والتبويبات |
| `--accent` | `#B45309` (كهرمان نحاسي) | `#F59E0B` (عنبر مشرق) | `#FEF08A` (شعاع ذهبي) | التمييز والعناصر البارزة والشارات |
| `--border` | `rgba(107, 17, 36, 0.15)` | `rgba(255, 255, 255, 0.1)` | `#4A371C` | الحدود وفواصل الأقسام |

---

## 3. الخطوط والتايبوغرافي الموحدة (Typography Tokens)

- **النصوص العربية العامة**: `var(--font-vazirmatn), 'Vazirmatn', sans-serif` لقراءة سلسة مريحة للعين في النصوص الطويلة.
- **النصوص والأسماء القبطية**: `'CopticStTakla', 'Noto Sans Coptic', sans-serif` للحفاظ على رسم الحروف القبطية الأصيلة وعلامات الجنكم والأشكال الكنسية.
- **النصوص الإنجليزية واللاتينية**: `var(--font-plus-jakarta), 'Plus Jakarta Sans', sans-serif`.
- **العناوين التراثية الإنجليزية**: `var(--font-libre-caslon), 'Libre Caslon Text', serif`.

---

## 4. القواعد الإلزامية لتطبيق الثيم على كل الصفحات

1. **الابتعاد التام عن الألوان العشوائية الصريحة**:
   - لا تستخدم `bg-red-500` أو `bg-blue-600` مباشرة إلا لدلالات حالة محددة (مثل الخطأ أو التأكيد)، والاعتماد دائماً على فئات Tailwind المربوطة بـ `amber`، `stone`، `zinc`، أو المتغيرات الأساسية (`bg-background`, `text-foreground`, `bg-card`).
2. **ضمان تباين النصوص (Contrast Ratio)**:
   - يجب ألا يقل معدل التباين بين النص والخلفية عن `4.5:1` لجميع الفقرات و `3:1` للعناوين الكبيرة لضمان الوصول الشامل (Accessibility 100%).
3. **تدرجات الألوان الزجاجية (Glassmorphism & Gradients)**:
   - استخدام `backdrop-blur-md` مع خلفيات شبه شفافة `bg-white/80 dark:bg-zinc-900/80` في الهيدر والبطاقات العائمة لإضفاء طابع فخم وحديث.
4. **تأثيرات التفاعل الحركي (Micro-interactions)**:
   - إضافة انتقالات ناعمة `transition-all duration-200` مع تأثيرات `hover:scale-[1.02]` للأزرار والبطاقات التفاعلية لتبدو الواجهة حية وجذابة.
